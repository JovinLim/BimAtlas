# Test Environment Configuration
# Used by pytest tests to connect to test database

# Test Database Configuration
TEST_DB_HOST=localhost
TEST_DB_PORT=5432
TEST_DB_NAME=bimatlas_test
TEST_DB_USER=bimatlas
TEST_DB_PASSWORD=bimatlas

# Note: Test database is separate from development/production databases
# to ensure tests do not affect real data.
# Password matches the POSTGRES_PASSWORD in infra/docker-compose.yml
